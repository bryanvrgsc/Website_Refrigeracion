# Dark Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Agregar modo oscuro con botón flotante (sistema/claro/oscuro) que adapta todo el sitio, incluido el logo SVG, sin flash de tema.

**Architecture:** Tokens semánticos en CSS (variables `--bg/--surface/--fg/--muted/--border`) mapeados a utilidades Tailwind v4 vía `@theme inline`, con clase `.dark` en `<html>`. Un script inline pre-paint aplica el tema desde `localStorage`+`matchMedia` y se reaplica en `astro:after-swap` (ClientRouter). Botón flotante cicla los 3 estados; el logo se cambia por swap de dos archivos SVG.

**Tech Stack:** Astro 6, Tailwind v4, vanilla TS en `<script>` de Astro. Sin test runner en el repo → verificación por `npm run build` limpio + chequeo manual en `astro dev`.

---

## Nota sobre verificación

El repo no tiene framework de tests. Cada tarea se verifica con:
- `npm run build` (debe terminar sin errores), y
- chequeo manual en `npm run dev` (`http://localhost:4321`) cuando aplica.

Los "Expected" describen lo observable. No inventar tests unitarios.

---

## File Structure

- `src/styles/global.css` — MODIFICAR: variante `dark`, tokens, `.glass`, patrones, scrollbar, selection.
- `src/scripts/theme.ts` — CREAR: helper de tema reutilizable (importado como módulo por el toggle).
- `src/layouts/Layout.astro` — MODIFICAR: script inline pre-paint, `color-scheme`, body a tokens, inyectar `ThemeToggle`, reaplicar en `astro:after-swap`.
- `src/components/ThemeToggle.astro` — CREAR: botón flotante.
- `public/favicon-dark.svg` — CREAR: logo con wordmark claro.
- `src/components/Header.astro` — MODIFICAR: tokens + swap de logo.
- Resto de componentes/páginas — MODIFICAR: colores fijos → tokens.

---

## Task 1: Variante dark + tokens semánticos en global.css

**Files:**
- Modify: `src/styles/global.css`

- [ ] **Step 1: Agregar la variante `dark` y los tokens**

Editar la parte superior de `src/styles/global.css`. Tras los `@import`, reemplazar el bloque `@theme` actual y el `@layer base` por:

```css
@import "tailwindcss";
@import "tailwind-animations";

@custom-variant dark (&:where(.dark, .dark *));

@theme {
  --color-primary: var(--color-blue-600);
  --color-secondary: var(--color-cyan-500);
}

@layer base {
  :root {
    color-scheme: light;
    --bg: var(--color-white);
    --surface: var(--color-white);
    --fg: var(--color-slate-900);
    --muted: var(--color-slate-600);
    --border: var(--color-slate-200);
    --pattern-dot: var(--color-slate-200);
    --pattern-dot-2: var(--color-slate-300);
  }

  .dark {
    color-scheme: dark;
    --bg: var(--color-slate-950);
    --surface: var(--color-slate-900);
    --fg: var(--color-slate-100);
    --muted: var(--color-slate-400);
    --border: var(--color-slate-800);
    --pattern-dot: var(--color-slate-800);
    --pattern-dot-2: var(--color-slate-700);
  }

  * {
    @apply border-border;
  }

  body {
    @apply bg-bg text-fg;
  }
}

@theme inline {
  --color-bg: var(--bg);
  --color-surface: var(--surface);
  --color-fg: var(--fg);
  --color-muted: var(--muted);
  --color-border: var(--border);
}
```

- [ ] **Step 2: Actualizar `.glass`, patrones, scrollbar y selection en el mismo archivo**

En `@layer utilities`, cambiar `.glass`:

```css
  .glass {
    @apply bg-surface/70 backdrop-blur-xl border border-border/40 shadow-sm;
  }
```

Cambiar los patrones para usar las vars:

```css
  .grid-pattern {
    background-image: radial-gradient(circle at 1px 1px, var(--pattern-dot) 1px, transparent 0);
    background-size: 40px 40px;
  }

  .dot-pattern {
    background-image: radial-gradient(var(--pattern-dot-2) 0.5px, transparent 0.5px);
    background-size: 20px 20px;
  }
```

Reemplazar el bloque de scrollbar (al final del archivo) por una versión con tokens:

```css
::-webkit-scrollbar {
  width: 10px;
}

::-webkit-scrollbar-track {
  @apply bg-surface;
}

::-webkit-scrollbar-thumb {
  @apply bg-border rounded-full border-4 border-solid border-surface hover:bg-muted/50 transition-colors;
}
```

- [ ] **Step 3: Verificar build**

Run: `npm run build`
Expected: termina sin errores. Las utilidades `bg-bg`, `bg-surface`, `text-fg`, `border-border` compilan.

- [ ] **Step 4: Commit**

```bash
git add src/styles/global.css
git commit -m "feat(theme): add dark variant and semantic color tokens"
```

---

## Task 2: Helper de tema `src/scripts/theme.ts`

**Files:**
- Create: `src/scripts/theme.ts`

- [ ] **Step 1: Crear el helper**

Crear `src/scripts/theme.ts`:

```ts
export type ThemeMode = "system" | "light" | "dark";

const STORAGE_KEY = "theme";
const LOGO_LIGHT = "/favicon.svg";
const LOGO_DARK = "/favicon-dark.svg";

export function getStored(): ThemeMode {
  if (typeof localStorage === "undefined") return "system";
  const v = localStorage.getItem(STORAGE_KEY);
  return v === "light" || v === "dark" ? v : "system";
}

export function setStored(mode: ThemeMode): void {
  localStorage.setItem(STORAGE_KEY, mode);
}

export function prefersDark(): boolean {
  return (
    typeof matchMedia !== "undefined" &&
    matchMedia("(prefers-color-scheme: dark)").matches
  );
}

export function resolveEffective(mode: ThemeMode): "light" | "dark" {
  if (mode === "system") return prefersDark() ? "dark" : "light";
  return mode;
}

export function applyTheme(mode: ThemeMode = getStored()): void {
  const effective = resolveEffective(mode);
  const root = document.documentElement;
  root.classList.toggle("dark", effective === "dark");

  const logo = document.getElementById("brand-logo") as HTMLImageElement | null;
  if (logo) logo.src = effective === "dark" ? LOGO_DARK : LOGO_LIGHT;

  document.dispatchEvent(
    new CustomEvent("themechange", { detail: { mode, effective } }),
  );
}

export function cycle(mode: ThemeMode): ThemeMode {
  return mode === "system" ? "light" : mode === "light" ? "dark" : "system";
}
```

- [ ] **Step 2: Verificar build**

Run: `npm run build`
Expected: sin errores (módulo TS válido, aún sin consumidores).

- [ ] **Step 3: Commit**

```bash
git add src/scripts/theme.ts
git commit -m "feat(theme): add reusable theme helper"
```

---

## Task 3: Script pre-paint + integración en Layout

**Files:**
- Modify: `src/layouts/Layout.astro`

- [ ] **Step 1: Agregar script inline pre-paint en `<head>`**

En `src/layouts/Layout.astro`, justo después de `<meta name="viewport" ... />` (línea ~21), agregar un script síncrono (sin `async`/`defer`, sin `is:inline` mal ubicado: debe correr antes del paint):

```astro
    <meta name="color-scheme" content="light dark" />
    <script is:inline>
      (function () {
        try {
          var stored = localStorage.getItem("theme");
          var mode = stored === "light" || stored === "dark" ? stored : "system";
          var dark =
            mode === "dark" ||
            (mode === "system" &&
              matchMedia("(prefers-color-scheme: dark)").matches);
          if (dark) document.documentElement.classList.add("dark");
        } catch (e) {}
      })();
    </script>
```

- [ ] **Step 2: Cambiar el body a tokens**

Reemplazar la clase del `<body>`:

```astro
  <body
    class="antialiased text-fg bg-bg selection:bg-blue-100 selection:text-blue-900 dark:selection:bg-blue-500/30 dark:selection:text-blue-100"
  >
```

- [ ] **Step 3: Reaplicar tema y logo en navegaciones ClientRouter**

Al final de `Layout.astro`, dentro del `<script>` existente (el del IntersectionObserver) o en un `<script>` nuevo de módulo, agregar:

```astro
<script>
  import { applyTheme } from "../scripts/theme.ts";

  document.addEventListener("astro:page-load", () => applyTheme());
  document.addEventListener("astro:after-swap", () => applyTheme());
  applyTheme();
</script>
```

- [ ] **Step 4: Verificar manual**

Run: `npm run dev`
Abrir `http://localhost:4321`. En DevTools setear `localStorage.theme = "dark"` y recargar.
Expected: `<html>` tiene clase `dark` desde el primer paint (sin flash claro). Navegar entre páginas mantiene `dark`.

- [ ] **Step 5: Commit**

```bash
git add src/layouts/Layout.astro
git commit -m "feat(theme): apply theme pre-paint and across navigations"
```

---

## Task 4: Logo oscuro `favicon-dark.svg`

**Files:**
- Create: `public/favicon-dark.svg`

- [ ] **Step 1: Generar el SVG oscuro desde el claro**

El wordmark usa `.st0 { fill: #060606; }`. Crear copia con ese fill en claro:

```bash
sed 's/fill: #060606/fill: #f1f5f9/' public/favicon.svg > public/favicon-dark.svg
```

- [ ] **Step 2: Verificar el cambio**

Run: `grep -E 'fill: #(060606|f1f5f9)' public/favicon-dark.svg`
Expected: aparece `fill: #f1f5f9` y NO aparece `fill: #060606`. (`.st1` blanco y `.st2` azul intactos.)

- [ ] **Step 3: Commit**

```bash
git add public/favicon-dark.svg
git commit -m "feat(theme): add dark-mode logo variant"
```

---

## Task 5: Swap de logo en Header

**Files:**
- Modify: `src/components/Header.astro:23-30`

- [ ] **Step 1: Dar id al `<img>` del logo**

En `src/components/Header.astro`, agregar `id="brand-logo"` al `<img>` del logo (línea ~23):

```astro
      <img
        id="brand-logo"
        src="/favicon.svg"
        alt="AC y Multiservicio Industrial"
        width="40"
        height="40"
        loading="eager"
        class="w-10 h-10 group-hover:scale-110 transition-transform duration-300"
      />
```

(El src lo ajusta `applyTheme` de Task 2/3; `src="/favicon.svg"` es el default seguro para SSR.)

- [ ] **Step 2: Verificar manual**

Run: `npm run dev`
Forzar `localStorage.theme="dark"`, recargar.
Expected: el logo del header muestra el wordmark claro (legible sobre fondo oscuro), sin parpadeo perceptible. En claro, wordmark oscuro normal.

- [ ] **Step 3: Commit**

```bash
git add src/components/Header.astro
git commit -m "feat(theme): swap header logo per active theme"
```

---

## Task 6: Botón flotante `ThemeToggle.astro`

**Files:**
- Create: `src/components/ThemeToggle.astro`
- Modify: `src/layouts/Layout.astro`

- [ ] **Step 1: Crear el componente**

Crear `src/components/ThemeToggle.astro`:

```astro
---
const labels = { system: "Sistema", light: "Claro", dark: "Oscuro" };
---

<button
  id="theme-toggle"
  type="button"
  aria-label={`Tema: ${labels.system}`}
  title={`Tema: ${labels.system}`}
  class="fixed bottom-5 right-5 z-50 w-12 h-12 grid place-items-center rounded-full glass text-fg shadow-xl active:scale-95 transition-transform hover:scale-105"
  style="padding-bottom: env(safe-area-inset-bottom, 0);"
>
  <!-- system: monitor -->
  <svg data-icon="system" xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <rect x="2" y="3" width="20" height="14" rx="2"></rect><line x1="8" y1="21" x2="16" y2="21"></line><line x1="12" y1="17" x2="12" y2="21"></line>
  </svg>
  <!-- light: sun -->
  <svg data-icon="light" class="hidden" xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <circle cx="12" cy="12" r="4"></circle><line x1="12" y1="2" x2="12" y2="4"></line><line x1="12" y1="20" x2="12" y2="22"></line><line x1="4.93" y1="4.93" x2="6.34" y2="6.34"></line><line x1="17.66" y1="17.66" x2="19.07" y2="19.07"></line><line x1="2" y1="12" x2="4" y2="12"></line><line x1="20" y1="12" x2="22" y2="12"></line><line x1="4.93" y1="19.07" x2="6.34" y2="17.66"></line><line x1="17.66" y1="6.34" x2="19.07" y2="4.93"></line>
  </svg>
  <!-- dark: moon -->
  <svg data-icon="dark" class="hidden" xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
  </svg>
</button>

<script>
  import { getStored, setStored, applyTheme, cycle, type ThemeMode } from "../scripts/theme.ts";

  const labels: Record<ThemeMode, string> = {
    system: "Sistema",
    light: "Claro",
    dark: "Oscuro",
  };

  function render(btn: HTMLElement, mode: ThemeMode) {
    btn.querySelectorAll<HTMLElement>("[data-icon]").forEach((icon) => {
      icon.classList.toggle("hidden", icon.dataset.icon !== mode);
    });
    const text = `Tema: ${labels[mode]}`;
    btn.setAttribute("aria-label", text);
    btn.setAttribute("title", text);
  }

  function initThemeToggle() {
    const btn = document.getElementById("theme-toggle");
    if (!btn || btn.dataset.bound === "true") {
      if (btn) render(btn, getStored());
      return;
    }
    btn.dataset.bound = "true";
    render(btn, getStored());

    btn.addEventListener("click", () => {
      const next = cycle(getStored());
      setStored(next);
      applyTheme(next);
    });

    document.addEventListener("themechange", (e) => {
      const detail = (e as CustomEvent).detail as { mode: ThemeMode };
      render(btn, detail.mode);
    });
  }

  document.addEventListener("astro:page-load", initThemeToggle);
  initThemeToggle();
</script>
```

- [ ] **Step 2: Inyectar en Layout**

En `src/layouts/Layout.astro`, importar y montar el componente. Agregar al frontmatter (junto a los otros imports):

```astro
import ThemeToggle from "../components/ThemeToggle.astro";
```

Y en el `<body>`, después de `<slot />` y antes de `<Analytics />`:

```astro
    <slot />
    <ThemeToggle />
    <Analytics />
```

- [ ] **Step 3: Verificar manual**

Run: `npm run dev`
Expected: botón circular abajo-derecha en todas las páginas. Click cicla Sistema→Claro→Oscuro→Sistema; el icono cambia (monitor/sol/luna), el sitio cambia de tema, persiste tras recargar. En "Sistema", cambiar el tema del SO refleja en vivo.

- [ ] **Step 4: Commit**

```bash
git add src/components/ThemeToggle.astro src/layouts/Layout.astro
git commit -m "feat(theme): add floating theme toggle button"
```

---

## Task 7: Convertir colores fijos — Header y Layout style global

**Files:**
- Modify: `src/components/Header.astro`
- Modify: `src/layouts/Layout.astro` (bloque `<style is:global>` si declara colores claros)

- [ ] **Step 1: Header — menú móvil y estados a tokens**

En `src/components/Header.astro`:
- Texto marca: `text-slate-900` → `text-fg` (línea ~21).
- Estado scroll en el `<script>` `syncHeaderState`: la clase `border-slate-200/50` → `border-border/50` (líneas ~143 y ~152).
- Menú móvil dropdown (línea ~96-97): `bg-white border-t border-gray-100` → `bg-surface border-t border-border`.
- Links del menú móvil (líneas ~106-108): activo `bg-blue-50 text-blue-600` → `bg-blue-500/10 text-blue-500`; inactivo `text-gray-600 hover:bg-gray-50` → `text-muted hover:bg-fg/5`.
- Divisor (línea ~115): `bg-gray-100` → `bg-border`.
- Hamburguesa (línea ~71): `text-slate-900` → `text-fg`.
- Nav desktop links inactivos (línea ~50): `text-slate-600 hover:text-blue-600 hover:bg-blue-50/50` → `text-muted hover:text-blue-500 hover:bg-blue-500/10`.

- [ ] **Step 2: Verificar build + manual**

Run: `npm run build`
Expected: sin errores. En dev, en dark: header glass oscuro, menú móvil con fondo `surface` oscuro, texto legible.

- [ ] **Step 3: Commit**

```bash
git add src/components/Header.astro src/layouts/Layout.astro
git commit -m "refactor(theme): migrate header colors to tokens"
```

---

## Task 8: Convertir colores fijos — componentes restantes

**Files:**
- Modify: `src/components/Footer.astro`, `Hero.astro`, `Services.astro`, `Benefits.astro`, `Brands.astro`, `Clients.astro`, `Suppliers.astro`, `Contact.astro`, `Quote.astro`, `Welcome.astro`, `MexicanFlag.astro`

- [ ] **Step 1: Localizar colores fijos**

Run: `grep -rnE 'bg-white|text-white|(bg|text|border|from|via|to|ring|divide)-(slate|gray)-[0-9]' src/components src/pages | grep -v ThemeToggle`
Expected: lista de ocurrencias a migrar.

- [ ] **Step 2: Migrar según reglas (archivo por archivo)**

Aplicar estas sustituciones, respetando el contraste (no tocar acentos azul/cyan ni gradientes de marca):
- Fondo de sección/página claro `bg-white` / `bg-slate-50*` → `bg-bg`.
- Tarjeta/superficie elevada `bg-white` sobre fondo → `bg-surface`.
- Texto principal `text-slate-900` / `text-slate-800` → `text-fg`.
- Texto secundario `text-slate-600` / `text-slate-500` / `text-gray-600` → `text-muted`.
- Bordes `border-slate-200` / `border-gray-100` → `border-border`.
- `text-white` que va sobre acento azul (botones) se mantiene (el azul no cambia).
- Para overlays/gradientes claros que rompan en dark, agregar `dark:` puntual (p. ej. `dark:from-slate-900`).

Hacer un commit por archivo o por grupo lógico para mantener pasos chicos. Ejemplo Footer:

```bash
# editar src/components/Footer.astro aplicando las reglas
npm run build
git add src/components/Footer.astro
git commit -m "refactor(theme): migrate footer colors to tokens"
```

Repetir para `Hero`, `Services`, `Benefits`, `Brands`, `Clients`, `Suppliers`, `Contact`, `Quote`, `Welcome`, `MexicanFlag`.

- [ ] **Step 3: Revisar logos de marca de bajo contraste**

`Brands.astro` / `Suppliers.astro` usan SVG de marcas (colores fijos). Si alguno queda invisible en dark, envolver su contenedor con `dark:bg-white/5 rounded-lg` (caso por caso). No alterar los SVG de terceros.

- [ ] **Step 4: Verificar manual por página**

Run: `npm run dev`
Recorrer `/`, `/sobre-nosotros`, `/servicios`, `/beneficios`, `/contacto`, `/cotizar`, `/404`, políticas. En dark y claro.
Expected: sin texto ilegible, sin "islas" blancas, bordes y glass coherentes, patrones tenues, logos visibles.

---

## Task 9: Verificación final

- [ ] **Step 1: Barrido de colores fijos restantes**

Run: `grep -rnE 'bg-white|text-slate-900|(text|bg|border)-(slate|gray)-[0-9]' src/components src/pages | grep -vE 'ThemeToggle|selection|blue|cyan'`
Expected: solo quedan casos intencionales (sobre acento azul) o `dark:` puntuales. Migrar lo que sobre.

- [ ] **Step 2: Build limpio**

Run: `npm run build`
Expected: sin errores ni warnings nuevos.

- [ ] **Step 3: Chequeo no-FOUC final**

Run: `npm run dev`
Con `localStorage.theme="dark"`, recarga dura (Cmd+Shift+R) en varias páginas.
Expected: nunca aparece destello de tema claro antes del oscuro. Navegación ClientRouter mantiene tema y logo.

- [ ] **Step 4: Commit final si hubo ajustes**

```bash
git add -A
git commit -m "fix(theme): final dark mode contrast and cleanup"
```

---

## Self-Review (cubierto)

- Spec §1 estados → Task 2 (`theme.ts`) + Task 6 (ciclo).
- Spec §2 tokens → Task 1.
- Spec §3 no-FOUC → Task 3 + Task 9.
- Spec §4 logo swap → Task 4 + Task 5.
- Spec §5 botón → Task 6.
- Spec §6 ajustes por componente → Task 7 + Task 8 + Task 9.
- Nombres consistentes: `applyTheme`, `getStored`, `setStored`, `cycle`, `resolveEffective`, `id="brand-logo"`, `id="theme-toggle"`, evento `themechange`.
```
